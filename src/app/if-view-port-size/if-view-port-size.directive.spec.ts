import { IfViewPortSizeDirective } from './if-view-port-size.directive';

describe('IfViewPortSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new IfViewPortSizeDirective();
    expect(directive).toBeTruthy();
  });
});
